Website:
 - create a docker container for Lambda (with larger indexes)
 - change query request from GET to POST
 - create a server-side session storing last questions and previous contex
 - create a textfield to easily modify prompts
 - update the GUI with an option to chat with two chatbots simultaneously
 - create logs in order to preserve some Q&A for validation

Loremaster:
 - give the model the bigger index
 - try the approach with an index made of different-sized chunks
 - test prompts with json-formatted sections

Vi & Jinx bots:
 - create input-output sets for fine-tuning
 - test the approach with dumping all the text in one prompt